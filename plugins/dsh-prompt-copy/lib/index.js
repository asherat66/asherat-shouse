'use strict';
// dsh-prompt-copy host half: the client half is DOM-injected and needs no
// server surface; this row only mounts the bundle and serves client.js.
exports.name = 'dsh-prompt-copy';
exports.inject = [];
exports.apply = function apply() {};
